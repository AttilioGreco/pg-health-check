---
postgres:
  host: localhost
  # i know, i know, the default port is 5432, on my machine for compatibility reasons i use 5433
  # i have to bind the port 5432 to 5433
  port: 5433
  user: postgres
  password: postgres
  dbname: postgres
server:
  port: 8080
  host: localhost
  # allowed
  # debug
  # release
  mode: debug
log:
# collector, console, syslog
  collector: console
  level: info
#  syslog:
#    host:         # syslog server host
#    local:        # local0 # available: local0, local1, local2, local3, local4, local5, local6, local7
#    protocol: tcp # tcp, udp
#    level: debug  # debug info warn error fatal panic